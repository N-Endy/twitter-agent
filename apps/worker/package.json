{
  "name": "@twitter-agent/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build --workspace @twitter-agent/core && tsx watch src/index.ts",
    "build": "npm run build --workspace @twitter-agent/core && tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "@twitter-agent/core": "file:../../packages/core",
    "bullmq": "latest",
    "date-fns": "latest",
    "ioredis": "latest",
    "tsx": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
