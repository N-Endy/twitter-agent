{
  "name": "@twitter-agent/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build --workspace @twitter-agent/core && tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "@twitter-agent/core": "file:../../packages/core",
    "bullmq": "latest",
    "date-fns": "latest",
    "ioredis": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
